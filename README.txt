whatsappreader
hackety hack hack hack a small "app" which let's you read whatsapp notifications without triggering the blue read icons

to install 
1) enable unknown sources
2) install 
3) enable whatsappreaderservice unter settings-> accessibility
4) keep your fingers crossed it works :D